/**
 * supabase.ts — DEPRECATED
 *
 * The Supabase SDK has been replaced by the FastAPI + PostgreSQL backend.
 * This file is kept to avoid import errors in case any legacy code references it,
 * but it is no longer used by any component.
 *
 * All data operations now go through: src/services/apiService.ts
 */

export const supabase = null as any;
